/* package com.example.PetAdoption.dominio.InterfRepositories;
import org.springframework.data.jpa.repository.JpaRepository;

import com.example.PetAdoption.dominio.entidades.OrgModel;

import java.util.*;
public interface OrgRepository extends JpaRepository<OrgModel, UUID> {
  Optional<OrgModel> findByEmail(String email);
} */